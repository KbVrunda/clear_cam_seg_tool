# Google Cloud service account JSON key path
# Need Access to the Key for Google Cloud
GOOGLE_APPLICATION_CREDENTIALS=/absolute/path/to/service-account.json

# Source bucket containing raw MP4 videos
GCS_VIDEO_BUCKET=your-video-bucket-name

# Destination bucket for annotated frames
GCS_ANNOTATION_BUCKET=your-annotation-bucket-name

# Optional: API server port
PORT=4000
